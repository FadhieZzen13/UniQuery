/**
 * Jest Global Setup — Frontend Tests
 * 
 * Loaded via `setupFilesAfterEnv` in jest.config.ts.
 * Extends Jest with DOM-specific matchers from @testing-library/jest-dom.
 */

import '@testing-library/jest-dom';
