const { Pool } = require('pg'); const dotenv = require('dotenv'); dotenv.config({path: './server/.env'}); const pool = new Pool({connectionString: process.env.DATABASE_URL}); pool.query(\INSERT INTO institutions (id, name, domain) VALUES ('a1ef33a4-8de7-4cd8-a7af-693939ed5171', 'Test University', 'test.edu') ON CONFLICT DO NOTHING\).then(res => { console.log('Done'); pool.end(); }).catch(err => { console.error(err); pool.end(); });
